<script type="text/javascript">
    (function($) {
        Vue.component('feedback-list', {

            template: '#vue-feedback-list',
            delimiters: ["${", "}"],
            props: ['feedbacks', 'isLastPage', 'authenticatedUser', 'itemAttributes', 'showControls', 'paddingClasses', 'paddingInlineStyles'],

            data: function()
            {
                return {};
            },

            methods: {
                onLoadMoreClicked: function()
                {
                    this.$emit('load-more');
                }
            }
        });
    })(jQuery);
</script>
