<script type="text/javascript">
    window.feedbackPublicPath = "{{ plugin_path('Feedback') ~ '/js/dist/' }}"
</script>

{% set minSuffix = '.min' %}
{% if ceresConfig.log.performanceLevel  == 'development' %}
    {% set minSuffix = '' %}
{% endif %}

<!-- SSR:entry({{ ssr_entry('Feedback', 'resources/js/dist/feedback-server' ~ minSuffix ~ '.js') }}) -->
<script src="{{ plugin_path('Feedback') }}/js/dist/feedback{{ minSuffix }}.js?v={{ buildHash }}"></script>
